<Window x:Class="PomodoroTimer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PomodoroTimer"
        mc:Ignorable="d"
        Title="Pomodoro Timer" 
        Height="500" Width="250"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="#E6171717" CornerRadius="20">
        <Grid>
            <StackPanel Orientation="Vertical" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Top" 
                        Margin="0,20,0,0"
                        Width="220">
                <TextBlock Text="Your future self is watching and thanking you." Style="{StaticResource QuoteTextStyle}"/>

                <TextBlock Text="15:49" Style="{StaticResource TimeTextStyle}"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top">
                    <Button x:Name="StartButton" Style="{StaticResource ControlButtons}"/>
                    <Button x:Name="StopButton" Style="{StaticResource ControlButtons}"/>
                </StackPanel>

                <Border Style="{StaticResource Divider}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
